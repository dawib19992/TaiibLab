import { ParserOsobyPipe } from './parser-osoby.pipe';

describe('ParserOsobyPipe', () => {
  it('create an instance', () => {
    const pipe = new ParserOsobyPipe();
    expect(pipe).toBeTruthy();
  });
});
