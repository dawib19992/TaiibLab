import { PodswietlenieDirective } from './podswietlenie.directive';

describe('PodswietlenieDirective', () => {
  it('should create an instance', () => {
    const directive = new PodswietlenieDirective();
    expect(directive).toBeTruthy();
  });
});
